# leaflet-study
study the usage of Leaflet and AMap api

## Usage

### Install

```shell
python -m pip install "PySide6-Fluent-Widgets[full]"

oython -m pip install requests

python -m pip install pydantic
```
